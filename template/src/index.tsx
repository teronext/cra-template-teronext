import React from 'react';
import ReactDOM from 'react-dom';
import 'style/index.scss';

ReactDOM.render(
  <React.StrictMode>
    <h1>Teronext template!</h1>
  </React.StrictMode>,
  document.getElementById('root')
);
